version: "2"

options:
  verbose: true
syncs:
  app_data:
    src: ${WORKDIR}
    sync_strategy: 'native_osx'
    sync_args:
    - "-prefer newer"
    - "-ignore='Path .git'"
    - "-ignore='BelowPath .git'"